machine:
# follow airgap documentation
# requires DNS and NTP servers
# https://www.talos.dev/latest/advanced/air-gapped/  
  registries: 
    mirrors:
      ghcr.io:
        endpoints:
          - http://${REGISTRY}:6000
      docker.io:
        endpoints:
          - http://${REGISTRY}:6000
      gcr.io:
        endpoints:
          - http://${REGISTRY}:6000
      registry.k8s.io:
        endpoints:
          - http://${REGISTRY}:6000